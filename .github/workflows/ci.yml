name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
    # paths: // path는 특정 폴더에 변경사항이 생겼을 때 작동하도록 설정할 수 있음
    #   - "services/**"

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '18'

      - name: Install Dependencies
        run: npm install

      - name: Build
        run: npm run build

  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '18'

      - name: Install Dependencies
        run: npm install

      - name: Run Test
        run: npm test
    deploy:
      needs: [build, test] # build와 test가 성공적으로 완료된 후에만 실행
      runs-on: ubuntu-latest
      if: github.event_name == 'push' && github.ref == 'refs/heads/main' # main 브랜치에 push될 때만 실행

      steps:
        - name: Checkout Repository
          uses: actions/checkout@v2

        - name: Deploy to Production
          run: |
            # 여기에 배포 스크립트를 추가
            # 예: npm run deploy
          env:
            # 필요한 환경 변수들 설정
